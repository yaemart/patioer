[build]
command = "pnpm install --frozen-lockfile && pnpm --filter @patioer/api build"

[deploy]
startCommand = "node apps/api/dist/server.js"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 30
