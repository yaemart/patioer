{
  "name": "@patioer/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "latest",
    "pg": "latest"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "drizzle-kit": "latest"
  },
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing/tenant-fixtures.ts"
  },
  "types": "./src/index.ts"
}
